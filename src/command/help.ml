let lily_h = {|usage: lily [command] [options]

Commands:

    build           Build project
    compile         Compile file (compiler)
    help            Print help
    init            Init project
    new             New project
    run             Run file (bytecode)
    test            Test file
    to              Transpile Lily to ...
    version         Print version

Options:

    -h, --help      Print help
    -v, --version   Print version
|}

let lily_build_h = {|usage: lily build [options]

Options:
    -h, --help      Print help
|}

let lily_compile_h = {|usage: lily compile [options] [file]

Options:
    -h, --help      Print help
|}

let lily_init_h = {|usage: lily init [options]

Options:
    -h, --help      Print help
|}

let lily_new_h = {|usage: lily new [options] [project_name]

Options:
    -h, --help      Print help
|}

let lily_run_h = {|usage: lily run [options] [file]

Options:
    -h, --help      Print help
|}

let lily_test_h = {|usage: lily test [options] [file]

Options:
    -h, --help      Print help
|}

let lily_to_h = {|usage: lily to [options] [file]

Options:
    -h, --help      Print help
|}
