let lily_h = {|usage: lily [command] [options]

Commands:

    build           Build project
    compile         Compile file (compiler)
    help            Print help
    init            Init project
    new             New project
    run             Run file (bytecode)
    to              Transpile Blade to ...
    version         Print version

Options:

    -h, --help      Print help
    -v, --version   Print version
|}
