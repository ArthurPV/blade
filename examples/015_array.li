(* 
array is default container 
*)
(* 
ex: [u32] is array 
*)

const io = import "Std.Io"

get_array :: [u32] -> [u32]
fun get_array a = a end

fun main = 
	io.println("{}", get_array([2,3,4,5])[0])
end
